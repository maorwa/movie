import { PostMoviePipe } from './post-movie.pipe';

describe('PostMoviePipe', () => {
  it('create an instance', () => {
    const pipe = new PostMoviePipe();
    expect(pipe).toBeTruthy();
  });
});
